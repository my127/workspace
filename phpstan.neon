parameters:
    level: 4
    paths:
        - src
        - tests
        - bin/workspace
        - config

    ignoreErrors:
    - '{Property .*Definition::\$.* is never written, only read}'
    - '{Property .*Container::\$parameters is never written, only read}'
